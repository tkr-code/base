<!DOCTYPE html>

<title>Options</title>

{% block body %}
    <h1>Options</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ option.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ option.name }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ option.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_options_index') }}">back to list</a>

    <a href="{{ path('app_admin_options_edit', {'id': option.id}) }}">edit</a>

    {{ include('admin/options/_delete_form.html.twig') }}
{% endblock %}
