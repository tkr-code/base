{% import "macros/link.html.twig" as link %}
{% import "macros/breadcrumb.html.twig" as breadcrumbs %}

{% block breadcrumb %}
{{ breadcrumbs.breadcrumb('Manage options'|trans,[{path:path('app_admin_options_index'),name:'Manage options'|trans
}
])}}
{% endblock %}

{% extends "admin/base.html.twig" %}
{% block title %}Edit options{% endblock %}

{% block body %}
    <div class="card card-outline card-success">
    <div class="card-body">
    <h1>Edit Options</h1>
        {{ include('admin/options/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('admin/options/_delete_form.html.twig') }}
    </div>
    </div>
    {{ link.back(path('app_admin_options_index'),'back to list') }}
{% endblock %}
