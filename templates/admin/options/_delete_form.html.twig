<form method="post" action="{{ path('app_admin_options_delete', {'id': option.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ option.id) }}">
    <button class="btn">Delete</button>
</form>
